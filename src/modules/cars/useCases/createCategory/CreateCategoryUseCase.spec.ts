import {} from 'jest';

describe('Create Category', () => {
  it('should be able to create a category', () => {
    const soma = 2 + 2;
    const resultado = 4;

    expect(soma).toBe(resultado);
  });
});
